# Agent-first browser
Displays web pages as an agent would see them.

Run via http server and see in regular browser (for humans)
```bash
npm run web-server
```

Run via console and see in terminal (for agents)
```bash
npm run console
```